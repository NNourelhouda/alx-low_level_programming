#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main()
{
    int x, y;
for (x = 0; x < 10; x++){
for (y = 0; y < 10;y++)
            {
                while (y!=x);
            putchar(x);
            putchar(y);
        }
}
        {
            putchar(',');
            putchar(' ');
        }
    putchar("\n");
    return 0;
}
